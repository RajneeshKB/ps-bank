import React, { FC } from 'react'

const AccountDashboard: FC = () => <div>Account dashboard</div>
// <Typography variant="h2">{`Welcome customer ${customerId} ${customerName}`}</Typography>

export default AccountDashboard
